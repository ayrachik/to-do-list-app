import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'doinsport-to-do-list',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
